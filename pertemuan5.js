let nilai =prompt("input nilai akhir :");
let grade = "";
fuction contoh IfElse()
{
	let nilai = prompt("input nilai akhir:");
	let grade = "";
// kondisi if else ="";
if (nilai >= 80) grade = "A"
else if (nilai >= 70) grade = "B"
else if (nilai >= 60) grade = "C"
else if (nilai >= 50) grade = "D"
	else grade = "E";

//document.write(`grade yang diperoleh adalah : ${grade}`);

document.write('grade yang diperoleh adalah: +{grade});
}
fuction contohswitchcase()
{
	let nilai = prompt(imput nilai akhir);
	let grade = "";
	switch(grade){

	case nilai >= 80:
	grade = "A";
	break;

	case nilai >= 70:
	grade = "B";
	break;

	switch(grade)
	case nilai >= 60:
	grade = "C";
	break;

	switch(grade)
	case nilai >= 50:
	grade = "D";
	break;

	default:
	grade = "E";
}
	document.write(`Grade yang diperoleh adalah : ' +grade);








